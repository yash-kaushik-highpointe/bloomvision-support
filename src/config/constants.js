export const ROUTES = {
  LOGIN: "/",
  DASHBOARD: "/dashboard",
};

export const AUTH = {
  TOKEN_KEY: "auth_token",
};

export const API = {
  BASE_URL: import.meta.env.VITE_API_BASE_URL || "http://localhost:3000",
};
